var express = require('express');
var router = express.Router();
var MongoClient=require('mongodb').MongoClient

/* GET users listing. */
router.get('/', function(req, res, next) {
    let 
  //res.render('browse', { title: 'Shop' });
});

module.exports = router;
